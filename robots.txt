User-agent: *
Allow: /

# The ad bridge pages under /go/ and the programme thank-you pages carry
# noindex, nofollow in the page itself. They are deliberately NOT disallowed
# here: a Disallow blocks the crawl, so the noindex is never read, and the
# URL can still be indexed from a link with no snippet.

Sitemap: https://wholesomegirlies.xyz/sitemap.xml
